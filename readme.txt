After clonining:

- Get all the dependencies for JS using npm, package.json ( npm install) 
- Get all the dependencies for PHP using Composer, composer.lock ( composer install)
- Use Symfony encore to buid up react files : npm run build 
 
